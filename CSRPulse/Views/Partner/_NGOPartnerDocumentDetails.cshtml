
@model Partner

@using CSRPulse.Common
<form data-ajax="true" data-ajax-method="POST" data-ajax-mode="replace" enctype="multipart/form-data" asp-action="SavePartnerDocument" asp-controller="Partner" data-ajax-success="onPartnerDocument">
    <div class="mb-2 text-end">
        <input type="hidden" asp-for="PartnerId" id="PartnerDocumentId" />
        <button class="btn btn-success btn-sm" type="submit" name="ButtonType" value="SavePartnerDocument"><i class="mdi mdi-file-document-edit"></i> Save</button>
    </div>
    <div class="row">
        <div class="mb-3">
            <table class="table table-hover table-sm table-centered mb-0">
                <thead class="table-light">
                    <tr>
                        <th>#</th>
                        <th>Document Type</th>
                        @*<th>File Name</th>*@
                        <th width="10%"></th>
                        <th>Remarks</th>
                        <th width="6%">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.PartnerDocument != null)
                    {
                        for (int i = 0; i < Model.PartnerDocument.Count; i++)
                        {
                            @*@if (Model != null)
                                {
                                    foreach (var item in Model)
                                    {*@
                            <tr>
                                <td>
                                    <span>@(i+1)</span>
                                    <input type="hidden" asp-for="@Model.PartnerDocument[i].PartnerDocumentId" />
                                    <input type="hidden" asp-for="@Model.PartnerDocument[i].PartnerId" />
                                    <input type="hidden" asp-for="@Model.PartnerDocument[i].ServerDocumentName" />
                                    @Model.PartnerDocument[i].DocumentName"
                                </td>
                                @*<td><span>@(Model.ToList().IndexOf(item) +1)</span></td>
                                    <td>@item.DocumentName</td>*@
                                @*<td>
                                        <a data-bs-toggle="tooltip" data-bs-placement="top" title="Download document" asp-controller="Partner" asp-action="DownloadDocument" asp-route-fileName="@item.ServerDocumentName">@item.DocumentName</a>
                                        <input type="hidden" asp-for="@item.UploadedDocumentName" />
                                    </td>*@
                                <td>
                                    <input asp-for="@Model.PartnerDocument[i].DocumentFile">
                                    @*<input asp-for="@item.DocumentFile">*@
                                </td>
                                <td>
                                    <input type="text" asp-for="@Model.PartnerDocument[i].Remark" class="form-control form-control-sm" maxlength="2000" placeholder="You can enter max 2000 characters" autocomplete="off">
                                    <span asp-validation-for="@Model.PartnerDocument[i].Remark" class="invalid"></span>

                                    @*<input type="text" asp-for="@item.Remark" class="form-control form-control-sm" maxlength="2000" placeholder="You can enter max 2000 characters" autocomplete="off">
                                        <span asp-validation-for="@item.Remark" class="invalid"></span>*@
                                </td>
                                <td class="text-center">
                                    @if (Model.PartnerDocument[i].ServerDocumentName != null && Model.PartnerDocument[i].PartnerDocumentId > 0)
                                    {
                                        <a class="font-16 delete-link action-icon" id="delete-link_@i" data-bs-toggle="tooltip" data-bs-placement="top" title="Click here, to delete record">
                                            <i class="dripicons-trash text-danger" aria-hidden="true"></i>
                                        </a>
                                        <div class="delete-confirm text-nowrap" id="delete-confirm_@i" style="display: none">
                                            <a class="confirm-delete text-danger" data-ajax="true" data-ajax-method="GET" data-ajax-update="#div-document-grid"
                                               data-ajax-mode="replace" asp-action="RemoveDocument" asp-route-pdId="@(Model.PartnerDocument[i].PartnerDocumentId)" asp-route-pId="@(Model.NgochartDocument[i].PartnerId)" asp-route-fName="@(Model.NgochartDocument[i].ServerDocumentName)" asp-controller="Partner">
                                                Yes
                                            </a> <text>|</text> <a class="delete-cancel text-dark" id="delete-cancel_@i" style="cursor: pointer">No</a>
                                        </div>
                                        @*@if (item.ServerDocumentName != null && item.PartnerDocumentId > 0)
                                            {*@
                                        @*<a class="font-16 delete-link action-icon" id="delete-link_@item" data-bs-toggle="tooltip" data-bs-placement="top" title="Click here, to delete record">
                                                <i class="dripicons-trash text-danger" aria-hidden="true"></i>
                                            </a>
                                            <div class="delete-confirm text-nowrap" id="delete-confirm_@item" style="display: none">
                                                <a class="confirm-delete text-danger" data-ajax="true" data-ajax-method="GET" data-ajax-update="#div-document-grid"
                                                   data-ajax-mode="replace" asp-action="RemoveDocument" asp-route-pdId="@(item.PartnerDocumentId)" asp-route-pId="@(item.PartnerId)" asp-route-fName="@(item.ServerDocumentName)" asp-controller="Partner">
                                                    Yes
                                                </a> <text>|</text> <a class="delete-cancel text-dark" id="delete-cancel_@item" style="cursor: pointer">No</a>
                                            </div>*@
                                    }
                                </td>
                                @*<td class="text-center">@ExtensionMethods.BoolToYesNo(item.IsUploaded)</td>
                                    <td class="text-center" data-bs-toggle="tooltip" data-bs-placement="top" title="Click here to, Upload Documents"><a id="documentId_@item.DocumentId" data-bs-toggle="modal" data-bs-target="#info-header-modal" onclick="addDocument(this)"><i class="uil-export me-2"></i></a></td>*@
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</form>

<script>
    function onChartDocument(data) {
        $("#div-ngopartnerdocment-grid").html(data.htmlData);
        if (data.flag == 1) {
            commonMessage('success', 'Partner Document Saved Successfully.');
        }


    }
    function addDocument(cnt) {
        //var type = cnt.id.split('_')[0];
        //var id = cnt.id.split('_')[1];
        //$('.btnconfirm').attr('id', type + '_' + id);
    }
</script>